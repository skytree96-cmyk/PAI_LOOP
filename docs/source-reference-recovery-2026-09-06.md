# Source header ownership and typed document references

Date: 2026-09-06

A current extraction can quote a valid inner performance cell instead of the criterion heading. The external whole-proposal minimum proof previously treated the separate heading score as unclaimed and used an inner-cell boundary for its census. Source reconstruction reproduced the failure with the same literal lengths observed in operator diagnostics.

The proof now independently locates one metric/maximum heading for every criterion, fences it at existing structural and blank boundaries, requires the original anchor inside that region, and exhaustively checks the same case rows there. Those proved headings participate in the unclaimed-score check. The first heading must still immediately follow the detailed-table marker. Original criterion literals, scope conditions, numerical values and scoring rules remain unchanged. Duplicate headings, foreign cutoffs and missing cases remain blocked.

Two exact typed gap grammars also recognize a notice that explicitly refers its full scoring table to an RFP, and an RFP that explicitly refers its submission date to the notice. A scoring-table gap still requires an AVAILABLE table in a different current RFP attachment. The date statement is irrelevant only to quantitative rule extraction; overall document closure still requires an effective NOTICE sibling. Compound and unreadable claims remain open.

Revalidation is targeted to MINIMUM_SCORE_EXCEEDS_TOTAL and EXTRACTION_DECLARED_INCOMPLETE records. Global prompt and validator versions do not change. Current-digest cached extraction can be revalidated without new provider calls when the normal cache proofs succeed.

Validation: 467 focused tests passed across quantitative rules, analysis pipeline, manual analysis and PPS enrichment. Real-source reconstruction with inner-cell anchors and the date-reference gap produced three AVAILABLE candidates; this is not a captured production model response or a verified company score. Full CI and live revalidation remain required.
