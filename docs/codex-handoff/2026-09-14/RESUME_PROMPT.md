# 다른 PC에서 사용할 재개 프롬프트

아래 내용을 새 Codex 작업에 그대로 붙여 넣으세요.

```text
PAI_LOOP의 최근 3년 낙찰 이력 수집 비용 개선 실험을 이어서 진행해줘.

저장소: https://github.com/skytree96-cmyk/PAI_LOOP
인수인계: docs/codex-handoff/2026-09-14/AWARD_COLLECTION_HANDOFF.md
문서 보관 브랜치: docs/award-collection-handoff-20260914
핵심 기존 배포: PR #160, 병합 커밋 1c39f47b3a6e947989ce6fe3da119e5b47fc4619

먼저 최신 main과 AGENTS.md, 인수인계 문서를 읽어. 문서가 main에 아직 없으면 보관 브랜치에서 읽고, 실제 구현은 최신 main 기반 별도 브랜치에서 해. 이전 PC의 .local 파일과 비밀 설정이 새 PC에 있다고 가정하지 마.

내 목표는 진행 중 공고에 최근 3년의 같은 실제 수요기관 + 사업 키워드 낙찰 이력을 연결하고, 신규 공고도 n8n 수집 후 자동 적용하면서 API 낭비를 줄이는 거야. 종료·취소 공고는 새 수집에서 제외하고 기존 기록은 보존해. 낙찰 수집에 Anthropic 등 유료 AI를 쓰지 말고, 반복 작업은 서버/n8n에서 돌려. Codex가 계속 폴링하며 토큰을 쓰지 마.

기관+키워드 자동화와 UI는 이미 배포됐지만 날짜별 공동 수집은 아직 실험이야. 기존 작업을 다시 배포하거나 처음부터 3년 전체를 재조회하지 마. 우선 현재 큐, 오늘 API 사용량·예약, W14 실행 상태를 읽기 전용으로 확인해. 문서의 수치를 현재 사실로 사용하지 마.

마지막 실험은 이래:
- 2025-03-17 개찰일 조건의 기본 낙찰 API 결과 4,668행을 999행씩 5회에 모두 받았고, 환경교육 한마당의 기존 낙찰을 재현했어.
- 전북특별자치도교육청 / (주)우리피플즈 / 44,000,000원 / 낙찰률 88% / 참가업체 1개는 동일했어. 내부 기관코드만 기존 null과 신규 값 차이가 있어. 업무 DB를 덮어쓰지 않았어.
- 농업마이스터 국외연수 사례는 2025-07-21 조회 전체 2,575행 중 999행만 받았어. 아직 불일치로 판정하면 안 돼.
- 이 실험 전체는 18호출이고, 종료 당시 2026-09-14 KST 장부가 1,000회였어. 공고 API와 유료 AI 호출은 이 실험에서 0회야. 현재 한도는 다시 확인해.

먼저 농업마이스터 비교를 끝내:
- 기본 경로 as/ScsbidInfoService/getScsbidListSttusServc
- inqryDiv=3, inqryBgnDt=202507210000, inqryEndDt=202507212359
- numOfRows=999, type=json; 기관·키워드 조건은 API 요청에 넣지 마.
- 검증된 기존 1페이지 캐시가 있으면 2·3페이지를 이어 받아. 캐시가 없으면 1~3페이지 전체를 소규모로 다시 받아.
- 기대 이력: 2025년 농업마이스터 지정자 국외연수 운영 / 농림수산식품교육문화정보원 / 지구촌농업교류센터 / 50,000,000원.
- 공고·차수·분류·재입찰 식별자로 기존 DB 이력과 연결해서 업체·금액·낙찰률·참가업체수·수요기관·날짜를 비교해.
- 요청은 실제 호출 전에 감사·예산에 반영하고 공고 API와 낙찰 API를 분리 집계해. 오류 자동 재시도는 0부터 시작해. 모든 페이지와 건수 일관성을 확인한 뒤에만 완료로 판정해.

중요한 함정:
- 운영은 공고게시일 기준이고 이번 실험은 개찰일 기준이므로 전체 3년 동등성을 바로 주장하면 안 돼.
- 개찰일 검색 조건과 응답 실개찰일이 다른 행이 많았어. 응답 opened_at으로 임의로 잘라 버리지 마.
- filter_notice_awards는 키워드 모든 단어 포함, 수집기 _fetch_window는 전체 문구 연속 부분일치야. 비교 필터를 고정해.
- 수요기관은 양쪽 코드가 있으면 코드 일치, 없으면 정규화한 기관명 정확 일치야. 공고기관으로 대체하지 마.
- 업체별 개찰 점수는 별도 호출이므로 최종낙찰 업체·금액 비교와 분리해.
- 로컬 스크립트는 캐시가 있으면 재실행을 막고, 그대로는 페이지 재개가 안 돼.

양성 비교를 끝낸 다음 같은 기관+키워드 캐시 공유, 기관별 공동 수집, 날짜별 공동 수집과 최근 변경분 수집의 실제 호출량을 비교해줘. 기존 3년 결과의 누락·추가·변경을 확인하고 나서 운영 전환을 구체화해. 비밀값이나 원본 응답을 GitHub·메일·출력에 노출하지 마. 운영 DB에 테스트·마이그레이션을 돌리지 마.

결과는 기존값과 새값이 같은지, 완료·미완료 범위, 총 실제 호출 수, 다음 필요한 작업만 짧게 알려줘.
```
