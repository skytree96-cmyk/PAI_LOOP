{
  "name": "PAI_LOOP 13 - Claude Extraction Gateway",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "pai-loop-claude/responses",
        "authentication": "headerAuth",
        "responseMode": "responseNode",
        "options": {
          "responseData": "firstEntryJson",
          "responseContentType": "application/json"
        }
      },
      "id": "claude-extraction-webhook",
      "name": "Claude Extraction Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        -520,
        0
      ],
      "webhookId": "c19fd695-9857-4f7f-af3b-6e28a72aa02e"
    },
    {
      "parameters": {
        "jsCode": "const body = $json?.body && typeof $json.body === 'object' && !Array.isArray($json.body) ? $json.body : null;\nif (!body) throw new Error('request body must be a JSON object');\nconst allowedTopLevel = ['input','max_output_tokens','model','service_tier','store','text'];\nconst actualTopLevel = Object.keys(body).sort();\nif (actualTopLevel.length !== allowedTopLevel.length || actualTopLevel.some((key, index) => key !== allowedTopLevel[index])) throw new Error('request fields do not match the extraction gateway contract');\nif (body.model !== 'claude-sonnet-5') throw new Error('model must be claude-sonnet-5');\nif (body.service_tier !== 'default' || body.store !== false) throw new Error('service_tier/store contract is invalid');\nif (!Number.isSafeInteger(body.max_output_tokens) || body.max_output_tokens < 256 || body.max_output_tokens > 20000) throw new Error('max_output_tokens is outside the bounded contract');\nif (!Array.isArray(body.input) || body.input.length !== 2) throw new Error('input must contain exactly system and user messages');\nconst readMessage = (item, role, maximum) => {\n  if (!item || item.role !== role || !Array.isArray(item.content) || item.content.length !== 1) throw new Error(`${role} message contract is invalid`);\n  const part = item.content[0];\n  if (!part || part.type !== 'input_text' || typeof part.text !== 'string') throw new Error(`${role} content must be one input_text part`);\n  const value = part.text;\n  if (!value.trim() || value.length > maximum || /[\\u0000]/.test(value)) throw new Error(`${role} content is empty, oversized, or unsafe`);\n  return value;\n};\nconst systemPrompt = readMessage(body.input[0], 'system', 12000);\nconst userPrompt = readMessage(body.input[1], 'user', 140000);\nif (!systemPrompt.startsWith('You extract procurement requirements as evidence only.')) throw new Error('system prompt identity is invalid');\nconst initialPrompt = userPrompt.startsWith('Allowed attachment IDs:');\nconst correctivePrompt = userPrompt.startsWith('FINAL CORRECTIVE RETRY.') && userPrompt.includes('Allowed attachment IDs:');\nif ((!initialPrompt && !correctivePrompt) || !userPrompt.includes('\\n\\nSOURCE:\\n')) throw new Error('user prompt identity is invalid');\nconst format = body.text?.format;\nif (!format || format.type !== 'json_schema' || format.name !== 'pai_loop_requirements' || format.strict !== true) throw new Error('strict response format is invalid');\nconst schema = format.schema;\nif (!schema || typeof schema !== 'object' || Array.isArray(schema) || schema.type !== 'object' || schema.additionalProperties !== false || !schema.properties || typeof schema.properties !== 'object' || !Array.isArray(schema.required)) throw new Error('response JSON schema boundary is invalid');\nconst schemaJson = JSON.stringify(schema);\nif (!schemaJson || schemaJson.length > 64000) throw new Error('response JSON schema is oversized');\nconst combinedCharacters = systemPrompt.length + userPrompt.length + schemaJson.length;\nif (combinedCharacters > 210000) throw new Error('combined Claude request is oversized');\nconst schemaInstruction = `\\n\\nReturn only one JSON object matching this schema exactly. Do not wrap it in Markdown. RESPONSE JSON SCHEMA:\\n${schemaJson}`;\nreturn [{ json: { system_prompt: systemPrompt, user_prompt: userPrompt + schemaInstruction, max_output_tokens: body.max_output_tokens } }];"
      },
      "id": "validate-claude-gateway-request",
      "name": "Validate Gateway Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -260,
        0
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "promptType": "define",
        "text": "={{ $json.user_prompt }}",
        "messages": {
          "messageValues": [
            {
              "message": "={{ $json.system_prompt }}"
            }
          ]
        },
        "batching": {}
      },
      "id": "claude-json-extraction",
      "name": "Claude JSON Extraction",
      "type": "@n8n/n8n-nodes-langchain.chainLlm",
      "typeVersion": 1.9,
      "position": [
        20,
        0
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "model": {
          "__rl": true,
          "mode": "list",
          "value": "claude-sonnet-5",
          "cachedResultName": "Claude Sonnet 5"
        },
        "options": {
          "maxTokensToSample": "={{ $json.max_output_tokens }}",
          "thinkingMode": "adaptive",
          "effort": "medium"
        }
      },
      "id": "claude-sonnet-5",
      "name": "Claude Sonnet 5",
      "type": "@n8n/n8n-nodes-langchain.lmChatAnthropic",
      "typeVersion": 1.5,
      "position": [
        20,
        220
      ]
    },
    {
      "parameters": {
        "jsCode": "const reject = (detail) => [{ json: { gateway_error: {\n  version: 'gateway-failure-v1', stage: 'OUTPUT_NORMALIZATION', code: 'OUTPUT_REJECTED',\n  upstream_http_status: null, detail_code: detail,\n} } }];\nconst rawOutput = $json?.text;\nif (rawOutput === undefined || rawOutput === null) return reject('OUTPUT_EMPTY');\nif (typeof rawOutput !== 'string') return reject('OUTPUT_TYPE_INVALID');\nif (rawOutput.length > 500000) return reject('OUTPUT_TOO_LARGE');\nif (!rawOutput.trim()) return reject('OUTPUT_EMPTY');\nlet outputText = rawOutput.trim();\nlet parsed;\ntry {\n  // JSON string values may contain Markdown markers; parse intact JSON first.\n  parsed = JSON.parse(outputText);\n} catch (ignored) {\n  if (!outputText.includes('```')) return reject('OUTPUT_JSON_INVALID');\n  // Preserve the single complete JSON fence policy, without prose.\n  const fenced = /^```(?:json)?\\r?\\n([\\s\\S]*?)\\r?\\n```$/.exec(outputText);\n  if (!fenced) return reject('OUTPUT_FENCE_INVALID');\n  outputText = fenced[1].trim();\n  try { parsed = JSON.parse(outputText); } catch (ignored) { return reject('OUTPUT_JSON_INVALID'); }\n}\nif (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) return reject('OUTPUT_NOT_OBJECT');\noutputText = JSON.stringify(parsed);\nlet tokenUsage = null;\ntry {\n  const rows = $('Claude Sonnet 5').all();\n  const candidate = rows.length ? rows[rows.length - 1]?.json?.tokenUsage : null;\n  if (candidate && typeof candidate === 'object') tokenUsage = candidate;\n} catch (ignored) {}\nconst counter = (value) => Number.isSafeInteger(value) && value >= 0 && value <= 10000000 ? value : null;\nconst inputTokens = counter(tokenUsage?.promptTokens);\nconst outputTokens = counter(tokenUsage?.completionTokens);\nconst totalTokens = counter(tokenUsage?.totalTokens);\nconst executionId = String($execution.id ?? '').replace(/[^A-Za-z0-9_-]/g, '').slice(0, 80);\nif (!executionId) return reject('EXECUTION_CONTEXT_INVALID');\nreturn [{ json: { id: `pai_claude_${executionId}`, status: 'completed', model: 'claude-sonnet-5', output_text: outputText, usage: { input_tokens: inputTokens, output_tokens: outputTokens, total_tokens: totalTokens } } }];"
      },
      "id": "normalize-claude-gateway-response",
      "name": "Normalize Gateway Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        300,
        0
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "const error = $json?.error;\nlet upstream = null;\nreturn [{ json: { gateway_error: { version: 'gateway-failure-v1', stage: 'INPUT_VALIDATION', code: 'REQUEST_REJECTED', upstream_http_status: upstream } } }];"
      },
      "id": "f555d877-26b4-502d-ad13-d91b9407cf77",
      "name": "Sanitize Gateway Input Failure",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        460,
        420
      ]
    },
    {
      "parameters": {
        "jsCode": "const error = $json?.error;\nlet upstream = null;\nif (error && typeof error === 'object' && !Array.isArray(error)) {\n  const candidates = [error.status, error.statusCode, error.httpCode];\n  const codes = candidates.filter(value => Number.isSafeInteger(value) && value >= 400 && value <= 599);\n  if (codes.length && codes.every(value => value === codes[0])) upstream = codes[0];\n}\nreturn [{ json: { gateway_error: { version: 'gateway-failure-v1', stage: 'MODEL_EXECUTION', code: 'MODEL_EXECUTION_FAILED', upstream_http_status: upstream } } }];"
      },
      "id": "e240578f-3227-5963-a952-916167574c33",
      "name": "Sanitize Gateway Model Failure",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        720,
        420
      ]
    },
    {
      "parameters": {
        "jsCode": "return [{ json: { gateway_error: { version: 'gateway-failure-v1', stage: 'OUTPUT_NORMALIZATION', code: 'OUTPUT_REJECTED', upstream_http_status: null, detail_code: 'NORMALIZER_EXCEPTION' } } }];"
      },
      "id": "eeece447-e965-53cc-a976-d3c463f2c23c",
      "name": "Sanitize Gateway Output Failure",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        980,
        420
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ (function guardGatewayResponse(value, successAllowed) {\n  const failure = () => ({ status: 500, body: { gateway_error: {\n    version: \"gateway-failure-v1\", stage: \"OUTPUT_NORMALIZATION\",\n    code: \"OUTPUT_REJECTED\", upstream_http_status: null, detail_code: \"TERMINAL_GUARD_REJECTED\",\n  } } });\n  const object = (item) => item !== null && typeof item === \"object\" && !Array.isArray(item);\n  const keys = (item, expected) => object(item)\n    && Object.keys(item).sort().join(\",\") === [...expected].sort().join(\",\");\n  const counter = (item) => item === null || item === undefined\n    || (Number.isSafeInteger(item) && item >= 0 && item <= 10000000);\n  try {\n    // Expected normalization failures travel through main[0] as a safe envelope.\n    // Both terminal lanes still rebuild it and can only return HTTP 500.\n    if (!successAllowed || (object(value) && Object.hasOwn(value, \"gateway_error\"))) {\n      if (!keys(value, [\"gateway_error\"])) return failure();\n      const item = value.gateway_error;\n      const fields = [\"version\", \"stage\", \"code\", \"upstream_http_status\"];\n      if (!keys(item, fields) && !keys(item, [...fields, \"detail_code\"])) return failure();\n      const pairs = { INPUT_VALIDATION: \"REQUEST_REJECTED\", MODEL_EXECUTION: \"MODEL_EXECUTION_FAILED\",\n        OUTPUT_NORMALIZATION: \"OUTPUT_REJECTED\" };\n      if (item.version !== \"gateway-failure-v1\" || !Object.hasOwn(pairs, item.stage)\n        || pairs[item.stage] !== item.code) return failure();\n      const status = item.upstream_http_status;\n      if (status !== null && (item.stage !== \"MODEL_EXECUTION\"\n        || !Number.isSafeInteger(status) || status < 400 || status > 599)) return failure();\n      const detail = item.detail_code;\n      const details = [\"OUTPUT_EMPTY\", \"OUTPUT_TYPE_INVALID\", \"OUTPUT_TOO_LARGE\",\n        \"OUTPUT_FENCE_INVALID\", \"OUTPUT_JSON_INVALID\", \"OUTPUT_NOT_OBJECT\",\n        \"EXECUTION_CONTEXT_INVALID\", \"NORMALIZER_EXCEPTION\", \"TERMINAL_GUARD_REJECTED\"];\n      if (detail !== undefined && detail !== null\n        && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      if (successAllowed && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      return { status: 500, body: { gateway_error: { version: \"gateway-failure-v1\",\n        stage: item.stage, code: item.code, upstream_http_status: status,\n        ...(detail === undefined || detail === null ? {} : { detail_code: detail }) } } };\n    }\n    if (!keys(value, [\"id\", \"status\", \"model\", \"output_text\", \"usage\"])\n      || value.status !== \"completed\" || value.model !== \"claude-sonnet-5\"\n      || typeof value.id !== \"string\" || !/^pai_claude_[A-Za-z0-9_-]{1,80}$/.test(value.id)\n      || typeof value.output_text !== \"string\" || !value.output_text.trim()\n      || value.output_text.length > 500000) return failure();\n    const parsed = JSON.parse(value.output_text);\n    if (!object(parsed)) return failure();\n    const usage = value.usage;\n    const usageKeys = [\"input_tokens\", \"output_tokens\", \"total_tokens\"];\n    if (!object(usage) || Object.keys(usage).some(key => !usageKeys.includes(key))\n      || !usageKeys.every(key => counter(usage[key]))) return failure();\n    return { status: 200, body: { id: value.id, status: \"completed\", model: \"claude-sonnet-5\",\n      output_text: JSON.stringify(parsed), usage: { input_tokens: usage.input_tokens ?? null,\n        output_tokens: usage.output_tokens ?? null, total_tokens: usage.total_tokens ?? null } } };\n  } catch (ignored) {\n    return failure();\n  }\n})($json, true).body }}",
        "options": {
          "responseCode": "={{ (function guardGatewayResponse(value, successAllowed) {\n  const failure = () => ({ status: 500, body: { gateway_error: {\n    version: \"gateway-failure-v1\", stage: \"OUTPUT_NORMALIZATION\",\n    code: \"OUTPUT_REJECTED\", upstream_http_status: null, detail_code: \"TERMINAL_GUARD_REJECTED\",\n  } } });\n  const object = (item) => item !== null && typeof item === \"object\" && !Array.isArray(item);\n  const keys = (item, expected) => object(item)\n    && Object.keys(item).sort().join(\",\") === [...expected].sort().join(\",\");\n  const counter = (item) => item === null || item === undefined\n    || (Number.isSafeInteger(item) && item >= 0 && item <= 10000000);\n  try {\n    // Expected normalization failures travel through main[0] as a safe envelope.\n    // Both terminal lanes still rebuild it and can only return HTTP 500.\n    if (!successAllowed || (object(value) && Object.hasOwn(value, \"gateway_error\"))) {\n      if (!keys(value, [\"gateway_error\"])) return failure();\n      const item = value.gateway_error;\n      const fields = [\"version\", \"stage\", \"code\", \"upstream_http_status\"];\n      if (!keys(item, fields) && !keys(item, [...fields, \"detail_code\"])) return failure();\n      const pairs = { INPUT_VALIDATION: \"REQUEST_REJECTED\", MODEL_EXECUTION: \"MODEL_EXECUTION_FAILED\",\n        OUTPUT_NORMALIZATION: \"OUTPUT_REJECTED\" };\n      if (item.version !== \"gateway-failure-v1\" || !Object.hasOwn(pairs, item.stage)\n        || pairs[item.stage] !== item.code) return failure();\n      const status = item.upstream_http_status;\n      if (status !== null && (item.stage !== \"MODEL_EXECUTION\"\n        || !Number.isSafeInteger(status) || status < 400 || status > 599)) return failure();\n      const detail = item.detail_code;\n      const details = [\"OUTPUT_EMPTY\", \"OUTPUT_TYPE_INVALID\", \"OUTPUT_TOO_LARGE\",\n        \"OUTPUT_FENCE_INVALID\", \"OUTPUT_JSON_INVALID\", \"OUTPUT_NOT_OBJECT\",\n        \"EXECUTION_CONTEXT_INVALID\", \"NORMALIZER_EXCEPTION\", \"TERMINAL_GUARD_REJECTED\"];\n      if (detail !== undefined && detail !== null\n        && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      if (successAllowed && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      return { status: 500, body: { gateway_error: { version: \"gateway-failure-v1\",\n        stage: item.stage, code: item.code, upstream_http_status: status,\n        ...(detail === undefined || detail === null ? {} : { detail_code: detail }) } } };\n    }\n    if (!keys(value, [\"id\", \"status\", \"model\", \"output_text\", \"usage\"])\n      || value.status !== \"completed\" || value.model !== \"claude-sonnet-5\"\n      || typeof value.id !== \"string\" || !/^pai_claude_[A-Za-z0-9_-]{1,80}$/.test(value.id)\n      || typeof value.output_text !== \"string\" || !value.output_text.trim()\n      || value.output_text.length > 500000) return failure();\n    const parsed = JSON.parse(value.output_text);\n    if (!object(parsed)) return failure();\n    const usage = value.usage;\n    const usageKeys = [\"input_tokens\", \"output_tokens\", \"total_tokens\"];\n    if (!object(usage) || Object.keys(usage).some(key => !usageKeys.includes(key))\n      || !usageKeys.every(key => counter(usage[key]))) return failure();\n    return { status: 200, body: { id: value.id, status: \"completed\", model: \"claude-sonnet-5\",\n      output_text: JSON.stringify(parsed), usage: { input_tokens: usage.input_tokens ?? null,\n        output_tokens: usage.output_tokens ?? null, total_tokens: usage.total_tokens ?? null } } };\n  } catch (ignored) {\n    return failure();\n  }\n})($json, true).status }}",
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json"
              },
              {
                "name": "Cache-Control",
                "value": "no-store"
              }
            ]
          }
        }
      },
      "id": "23f863d5-2622-5c5a-bec4-537693799841",
      "name": "Respond Gateway Success",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        1300,
        140
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ (function guardGatewayResponse(value, successAllowed) {\n  const failure = () => ({ status: 500, body: { gateway_error: {\n    version: \"gateway-failure-v1\", stage: \"OUTPUT_NORMALIZATION\",\n    code: \"OUTPUT_REJECTED\", upstream_http_status: null, detail_code: \"TERMINAL_GUARD_REJECTED\",\n  } } });\n  const object = (item) => item !== null && typeof item === \"object\" && !Array.isArray(item);\n  const keys = (item, expected) => object(item)\n    && Object.keys(item).sort().join(\",\") === [...expected].sort().join(\",\");\n  const counter = (item) => item === null || item === undefined\n    || (Number.isSafeInteger(item) && item >= 0 && item <= 10000000);\n  try {\n    // Expected normalization failures travel through main[0] as a safe envelope.\n    // Both terminal lanes still rebuild it and can only return HTTP 500.\n    if (!successAllowed || (object(value) && Object.hasOwn(value, \"gateway_error\"))) {\n      if (!keys(value, [\"gateway_error\"])) return failure();\n      const item = value.gateway_error;\n      const fields = [\"version\", \"stage\", \"code\", \"upstream_http_status\"];\n      if (!keys(item, fields) && !keys(item, [...fields, \"detail_code\"])) return failure();\n      const pairs = { INPUT_VALIDATION: \"REQUEST_REJECTED\", MODEL_EXECUTION: \"MODEL_EXECUTION_FAILED\",\n        OUTPUT_NORMALIZATION: \"OUTPUT_REJECTED\" };\n      if (item.version !== \"gateway-failure-v1\" || !Object.hasOwn(pairs, item.stage)\n        || pairs[item.stage] !== item.code) return failure();\n      const status = item.upstream_http_status;\n      if (status !== null && (item.stage !== \"MODEL_EXECUTION\"\n        || !Number.isSafeInteger(status) || status < 400 || status > 599)) return failure();\n      const detail = item.detail_code;\n      const details = [\"OUTPUT_EMPTY\", \"OUTPUT_TYPE_INVALID\", \"OUTPUT_TOO_LARGE\",\n        \"OUTPUT_FENCE_INVALID\", \"OUTPUT_JSON_INVALID\", \"OUTPUT_NOT_OBJECT\",\n        \"EXECUTION_CONTEXT_INVALID\", \"NORMALIZER_EXCEPTION\", \"TERMINAL_GUARD_REJECTED\"];\n      if (detail !== undefined && detail !== null\n        && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      if (successAllowed && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      return { status: 500, body: { gateway_error: { version: \"gateway-failure-v1\",\n        stage: item.stage, code: item.code, upstream_http_status: status,\n        ...(detail === undefined || detail === null ? {} : { detail_code: detail }) } } };\n    }\n    if (!keys(value, [\"id\", \"status\", \"model\", \"output_text\", \"usage\"])\n      || value.status !== \"completed\" || value.model !== \"claude-sonnet-5\"\n      || typeof value.id !== \"string\" || !/^pai_claude_[A-Za-z0-9_-]{1,80}$/.test(value.id)\n      || typeof value.output_text !== \"string\" || !value.output_text.trim()\n      || value.output_text.length > 500000) return failure();\n    const parsed = JSON.parse(value.output_text);\n    if (!object(parsed)) return failure();\n    const usage = value.usage;\n    const usageKeys = [\"input_tokens\", \"output_tokens\", \"total_tokens\"];\n    if (!object(usage) || Object.keys(usage).some(key => !usageKeys.includes(key))\n      || !usageKeys.every(key => counter(usage[key]))) return failure();\n    return { status: 200, body: { id: value.id, status: \"completed\", model: \"claude-sonnet-5\",\n      output_text: JSON.stringify(parsed), usage: { input_tokens: usage.input_tokens ?? null,\n        output_tokens: usage.output_tokens ?? null, total_tokens: usage.total_tokens ?? null } } };\n  } catch (ignored) {\n    return failure();\n  }\n})($json, false).body }}",
        "options": {
          "responseCode": "={{ (function guardGatewayResponse(value, successAllowed) {\n  const failure = () => ({ status: 500, body: { gateway_error: {\n    version: \"gateway-failure-v1\", stage: \"OUTPUT_NORMALIZATION\",\n    code: \"OUTPUT_REJECTED\", upstream_http_status: null, detail_code: \"TERMINAL_GUARD_REJECTED\",\n  } } });\n  const object = (item) => item !== null && typeof item === \"object\" && !Array.isArray(item);\n  const keys = (item, expected) => object(item)\n    && Object.keys(item).sort().join(\",\") === [...expected].sort().join(\",\");\n  const counter = (item) => item === null || item === undefined\n    || (Number.isSafeInteger(item) && item >= 0 && item <= 10000000);\n  try {\n    // Expected normalization failures travel through main[0] as a safe envelope.\n    // Both terminal lanes still rebuild it and can only return HTTP 500.\n    if (!successAllowed || (object(value) && Object.hasOwn(value, \"gateway_error\"))) {\n      if (!keys(value, [\"gateway_error\"])) return failure();\n      const item = value.gateway_error;\n      const fields = [\"version\", \"stage\", \"code\", \"upstream_http_status\"];\n      if (!keys(item, fields) && !keys(item, [...fields, \"detail_code\"])) return failure();\n      const pairs = { INPUT_VALIDATION: \"REQUEST_REJECTED\", MODEL_EXECUTION: \"MODEL_EXECUTION_FAILED\",\n        OUTPUT_NORMALIZATION: \"OUTPUT_REJECTED\" };\n      if (item.version !== \"gateway-failure-v1\" || !Object.hasOwn(pairs, item.stage)\n        || pairs[item.stage] !== item.code) return failure();\n      const status = item.upstream_http_status;\n      if (status !== null && (item.stage !== \"MODEL_EXECUTION\"\n        || !Number.isSafeInteger(status) || status < 400 || status > 599)) return failure();\n      const detail = item.detail_code;\n      const details = [\"OUTPUT_EMPTY\", \"OUTPUT_TYPE_INVALID\", \"OUTPUT_TOO_LARGE\",\n        \"OUTPUT_FENCE_INVALID\", \"OUTPUT_JSON_INVALID\", \"OUTPUT_NOT_OBJECT\",\n        \"EXECUTION_CONTEXT_INVALID\", \"NORMALIZER_EXCEPTION\", \"TERMINAL_GUARD_REJECTED\"];\n      if (detail !== undefined && detail !== null\n        && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      if (successAllowed && (item.stage !== \"OUTPUT_NORMALIZATION\" || !details.includes(detail))) return failure();\n      return { status: 500, body: { gateway_error: { version: \"gateway-failure-v1\",\n        stage: item.stage, code: item.code, upstream_http_status: status,\n        ...(detail === undefined || detail === null ? {} : { detail_code: detail }) } } };\n    }\n    if (!keys(value, [\"id\", \"status\", \"model\", \"output_text\", \"usage\"])\n      || value.status !== \"completed\" || value.model !== \"claude-sonnet-5\"\n      || typeof value.id !== \"string\" || !/^pai_claude_[A-Za-z0-9_-]{1,80}$/.test(value.id)\n      || typeof value.output_text !== \"string\" || !value.output_text.trim()\n      || value.output_text.length > 500000) return failure();\n    const parsed = JSON.parse(value.output_text);\n    if (!object(parsed)) return failure();\n    const usage = value.usage;\n    const usageKeys = [\"input_tokens\", \"output_tokens\", \"total_tokens\"];\n    if (!object(usage) || Object.keys(usage).some(key => !usageKeys.includes(key))\n      || !usageKeys.every(key => counter(usage[key]))) return failure();\n    return { status: 200, body: { id: value.id, status: \"completed\", model: \"claude-sonnet-5\",\n      output_text: JSON.stringify(parsed), usage: { input_tokens: usage.input_tokens ?? null,\n        output_tokens: usage.output_tokens ?? null, total_tokens: usage.total_tokens ?? null } } };\n  } catch (ignored) {\n    return failure();\n  }\n})($json, false).status }}",
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json"
              },
              {
                "name": "Cache-Control",
                "value": "no-store"
              }
            ]
          }
        }
      },
      "id": "6b232e20-2b85-52d9-9701-440754da52be",
      "name": "Respond Gateway Failure",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        1300,
        420
      ]
    }
  ],
  "connections": {
    "Claude Extraction Webhook": {
      "main": [
        [
          {
            "node": "Validate Gateway Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Gateway Request": {
      "main": [
        [
          {
            "node": "Claude JSON Extraction",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Sanitize Gateway Input Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claude Sonnet 5": {
      "ai_languageModel": [
        [
          {
            "node": "Claude JSON Extraction",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    },
    "Claude JSON Extraction": {
      "main": [
        [
          {
            "node": "Normalize Gateway Response",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Sanitize Gateway Model Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sanitize Gateway Input Failure": {
      "main": [
        [
          {
            "node": "Respond Gateway Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sanitize Gateway Model Failure": {
      "main": [
        [
          {
            "node": "Respond Gateway Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize Gateway Response": {
      "main": [
        [
          {
            "node": "Respond Gateway Success",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Sanitize Gateway Output Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Sanitize Gateway Output Failure": {
      "main": [
        [
          {
            "node": "Respond Gateway Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveDataErrorExecution": "none",
    "saveDataSuccessExecution": "none",
    "saveManualExecutions": false,
    "timezone": "Asia/Seoul"
  }
}
